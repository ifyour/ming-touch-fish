{
  "name": "@repo/fetcher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/ingest": "workspace:*",
    "@repo/shared": "workspace:*",
    "@repo/telemetry": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250100.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.107.0"
  }
}
